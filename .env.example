# TrustLink Environment Variables
# Copy this file to .env and fill in your actual values

# ======================
# Existing TrustLink Config
# ======================

# Paystack API
PAYSTACK_SECRET_KEY=sk_test_your_paystack_secret_key_here

# Database
DATABASE_URL=postgresql://billvianney:@localhost:5432/trustlink_waitlist

# Server
BASE_URL=http://localhost:3000
PORT=3000
NODE_ENV=development

# Security
JWT_SECRET=your_jwt_secret_here_use_openssl_rand_hex_32
QR_SECRET_KEY=your_qr_secret_here_use_openssl_rand_hex_32

# ======================
# NEW: Waitlist Config
# ======================

# Admin dashboard access key
# Generate with: openssl rand -hex 32
ADMIN_KEY=your_admin_key_here_keep_this_secret

# ======================
# Optional: Future Features
# ======================

# Email notifications (for future implementation)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# SMS notifications (for future implementation)
# TWILIO_ACCOUNT_SID=your_twilio_account_sid
# TWILIO_AUTH_TOKEN=your_twilio_auth_token
# TWILIO_PHONE_NUMBER=+1234567890

# ======================
# Production Notes
# ======================

# When deploying to production (e.g., Render.com):
# 1. Set NODE_ENV=production
# 2. Use HTTPS for BASE_URL (e.g., https://trustlink.onrender.com)
# 3. Generate strong random values for all secrets
# 4. Never commit this file to git (it's in .gitignore)
# 5. Set environment variables in your hosting platform

# ======================
# Quick Secret Generation
# ======================

# Generate all secrets at once:
# echo "JWT_SECRET=$(openssl rand -hex 32)"
# echo "QR_SECRET_KEY=$(openssl rand -hex 32)"
# echo "ADMIN_KEY=$(openssl rand -hex 32)"